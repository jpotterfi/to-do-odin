//build ToDo factory and then a function
//that takes keys and values from local storage
//and then use factory to build
//and then render

function toDoFactory () {
    this.title = title;
    this.dueDate = dueDate;
    this.priotiy = priority;
    //figure out how to add protypical inheritance with factory
    //connect toDo to a project
}